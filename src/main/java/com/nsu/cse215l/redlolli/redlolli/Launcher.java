package com.nsu.cse215l.redlolli.redlolli;

import javafx.application.Application;
import com.nsu.cse215l.redlolli.redlolli.core.GameLogger;

/**
 * Operates as the foundational systemic bootstrap vector instantiating absolute
 * environmental parameters statically.
 * Isolates explicit virtual machine execution points guaranteeing synchronized
 * hardware-accelerated pipeline initialization optimally.
 */
public class Launcher {

    /**
     * Executes explicit runtime initialization procedures delegating continuous
     * synchronous rendering threads to external APIs natively.
     * Integrates diagnostic logging configurations globally prior to geometric
     * overlay instantiations.
     * 
     * @param args Exogenous string arrays mapping command-line scalar
     *             configurations unconditionally.
     */
    public static void main(String[] args) {
        // Enforces structural deterministic trace outputs definitively standardizing
        // execution events synchronously
        GameLogger.configure();

        // Transfers overarching thread management delegating persistent lifecycle
        // parameters explicitly
        Application.launch(HelloApplication.class, args);
    }
}
